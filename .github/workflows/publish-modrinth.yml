name: Publish to Modrinth

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 25

      # gradle.properties pins org.gradle.java.home to a local macOS path; drop it so the CI JDK is used.
      - name: Use the CI JDK
        run: sed -i '/org.gradle.java.home/d' gradle.properties

      - name: Build
        run: |
          chmod +x gradlew
          ./gradlew build

      # On a real release use the tag; on a manual dispatch fall back to the version in gradle.properties
      # so the Modrinth version is never named after the branch.
      - name: Resolve version
        id: ver
        run: |
          if [ -n "$TAG" ]; then
            echo "value=${TAG#v}" >> "$GITHUB_OUTPUT"
          else
            echo "value=$(grep '^mod_version=' gradle.properties | cut -d= -f2)+26.1.2" >> "$GITHUB_OUTPUT"
          fi
        env:
          TAG: ${{ github.event.release.tag_name }}

      - uses: Kir-Antipov/mc-publish@v3.3
        with:
          modrinth-id: aqnMGg9O
          modrinth-token: ${{ secrets.MODRINTH_TOKEN }}
          # The token only needs "Create versions"; skip unfeaturing older versions (which needs write scope).
          modrinth-unfeature-mode: none
          files: build/libs/!(*-sources).jar
          name: Minecarts Overhaul ${{ steps.ver.outputs.value }}
          version: ${{ steps.ver.outputs.value }}
          version-type: beta
          loaders: fabric
          game-versions: 26.1.2
          dependencies: |
            fabric-api(required)
            addadd(optional)
